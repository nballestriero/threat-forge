# Minimal Governed Child Project Demo

This directory is a versioned seed used by threat-forge to create a resettable demo child-project workspace.

The seed is not the live demo instance. Runtime reset commands copy it into `.threat-forge/workspaces/demo-child-project/`, where users can experiment without mutating the governed seed.
