# MR-0002ADR-0003REQ-0001GOV-0001 — Creazione atomica, non distruttiva e stato scaffolded

## Intent

Impedire che la creazione guidata lasci file scollegati, record orfani o implementazioni incomplete marcate come completate.

## Governance obligation

- The governed implementation must enforce this condition: richiedere una conferma esplicita di scrittura.
- The governed implementation must enforce this condition: riusare il contratto di validazione del pianificatore.
- The governed implementation must enforce this condition: rifiutare un path già presente nel filesystem.
- The governed implementation must enforce this condition: rifiutare un path già citato nell'implementation trace registry.
- The governed implementation must enforce this condition: creare il file con semantica create-only.
- The governed implementation must enforce this condition: registrare lo stato `scaffolded` e il campo `scaffolded_path`.
- The governed implementation must enforce this condition: inserire `@implementationStatus scaffolded` nei sorgenti creati.
- The governed implementation must enforce this condition: ripristinare lo stato precedente se file e registro non possono essere aggiornati entrambi.
- The governed implementation must enforce this condition: non eseguire operazioni Git.

## Verification obligations

- The verification must confirm this condition: requisito esistente.
- The verification must confirm this condition: tipo sorgente supportato.
- The verification must confirm this condition: path relativo e sicuro.
- The verification must confirm this condition: titolo su una sola riga.
- The verification must confirm this condition: conferma esatta `create`.
- The verification must confirm this condition: identificativo implementativo non duplicato.
- The verification must confirm this condition: file e record coerenti.
- The verification must confirm this condition: nessun residuo temporaneo dopo successo o fallimento.

## Failure conditions

- The verification must fail when any governed obligation is violated or required deterministic evidence is missing.
